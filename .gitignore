# Cloudflare origin certificates (per-domain, generated on deployment)
nginx/ssl/*/origin.pem
nginx/ssl/*/origin.key

# Fallback self-signed cert (generated by setup.sh)
nginx/ssl/fallback.crt
nginx/ssl/fallback.key

# Site configs are generated per-deployment by add-site.sh
nginx/sites/*.conf
